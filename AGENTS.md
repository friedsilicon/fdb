# FDB Agent Guide

Start with [docs/ai/project-context.md](docs/ai/project-context.md).

This root file exists for tool discovery. The detailed, canonical repo context
lives in `docs/ai/project-context.md`.

Keep this file lightweight. Only add root-level notes here when a tool needs
something discoverable at the repository root.
